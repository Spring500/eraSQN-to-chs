@GET_TREASURE_EXPLORER
#DIM LCOUNT
VARSET LOCAL
;アイテムを持てるかチェック
FOR LCOUNT, 0, 50
	SIF LCOUNT <= 29 && ITEM:LCOUNT
		CONTINUE
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 96, 100
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 300, 330
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT

SIF ITEM:淫魔水滴 + I:78 < 999
	LOCAL:78 = 1


CALL SET_PALAM_EXPLORER, "財宝期待値", RAND:100
SELECTCASE PALAM_EXPLORER("アイテムレベル")
CASE 1
	IF PALAM_EXPLORER("財宝期待値") >= 61 && PALAM_EXPLORER("財宝期待値") <= 70 && LOCAL:40
		CALL PRINT_COLORTEXT, "茉莉薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:40 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 71 && PALAM_EXPLORER("財宝期待値") <= 80 && LOCAL:41
		CALL PRINT_COLORTEXT, "薰衣草薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:41 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 81 && PALAM_EXPLORER("財宝期待値") <= 85 && LOCAL:43
		CALL PRINT_COLORTEXT, "檀香薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:43 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 86 && PALAM_EXPLORER("財宝期待値") <= 90 && LOCAL:44
		CALL PRINT_COLORTEXT, "迷迭香薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:44 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") <= 90
		MONEY:5 = PALAM_EXPLORER("財宝期待値")
		CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("黄色")
		PRINTFORML 拾いました。
		MONEY:6 += MONEY:5
	ELSE
		CALL TREASURE_BOX_EXPLORER
	ENDIF
CASEELSE
	IF PALAM_EXPLORER("財宝期待値") >= 11 && PALAM_EXPLORER("財宝期待値") <= 90
		CALL TREASURE_BOX_EXPLORER
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 91 && PALAM_EXPLORER("財宝期待値") <= 92 && LOCAL:40
		CALL PRINT_COLORTEXT, "茉莉薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:40 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 93 && PALAM_EXPLORER("財宝期待値") <= 94 && LOCAL:41
		CALL PRINT_COLORTEXT, "薰衣草薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:41 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 95 && PALAM_EXPLORER("財宝期待値") <= 96 && LOCAL:43
		CALL PRINT_COLORTEXT, "檀香薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:43 += 1
	ELSEIF PALAM_EXPLORER("財宝期待値") >= 97 && LOCAL:44
		CALL PRINT_COLORTEXT, "迷迭香薰香", DEF_COLOR("黄色")
		PRINTFORML を摘みました。
		I:44 += 1
	ELSE
		MONEY:5 = PALAM_EXPLORER("財宝期待値") + 100
		CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("黄色")
		PRINTFORML 拾いました。
		MONEY:6 += MONEY:5
	ENDIF
ENDSELECT
WAIT

;宝箱
@TREASURE_BOX_EXPLORER

;歌声のトラップ
IF TEXTS("探索先") == "アンショス海" && RAND:5 == 0
	PRINT 何処からか歌声が聞こえてくる。これは…

	SELECTCASE RAND:5
	CASE 3, 4
		PRINT 誘惑の歌だ！
		IF RAND:100 < 80
			IF RAND:2
				PRINTFORML …蟲惑的な歌声が%CALLNAME:PLAYER%の精力を蝕む…
				BASE:PLAYER:精力 -= RAND:6 + 6
			ELSE
				PRINTFORML …%CALLNAME:PLAYER%はその誘惑に逆らえなかった…
				BASE:PLAYER:精力 -= RAND:4 + 3
			ENDIF
		ELSE
			PRINTFORML …だが%CALLNAME:PLAYER%はその誘惑を振り切った
		ENDIF
	CASE 1, 2
		IF RAND:10 == 0 && PALAM_EXPLORER("フロア") < PALAM_EXPLORER("最深フロア")
			PRINTFORML いざないの歌だ！…美しい歌声が%CALLNAME:PLAYER%を奥へ導く…
			CALL SET_PALAM_EXPLORER, "フロア", 1
			PRINTFORM {PALAM_EXPLORER("フロア")}Ｆ探索開始…
		ELSE
			PRINT 惑いの歌だ！
			IF RAND:5 >= 2 && PALAM_EXPLORER("フロア") > 1
				PRINTFORML …%CALLNAME:PLAYER%は歌声に惑わされて道を引き返してしまった…
				CALL SET_PALAM_EXPLORER, "フロア", -1
				PRINTFORM 再び{PALAM_EXPLORER("フロア")}Ｆ探索開始…
			ELSEIF RAND:4 == 0 && PALAM_EXPLORER("フロア") > 4
				PRINTFORML
				PRINTFORML ……
				PRINTFORML ………
				PRINTFORML 気が付くと、%CALLNAME:PLAYER%は館の前にいた…
				COUNT:99 = 100
			ELSE
				PRINTFORML …しかし%CALLNAME:PLAYER%は惑わされることなく、再び奥へ向かって歩き始めた…
			ENDIF
		ENDIF
	CASEELSE
		PRINT 平穏の歌だ！
		IF RAND:2
			PRINTFORML …美しい歌声は%CALLNAME:PLAYER%の体に安らぎをもたらした…
			BASE:PLAYER:精力 = MIN(BASE:PLAYER:精力 + 4, MAXBASE:PLAYER:精力)
		ELSE
			PRINTFORML …歌声に秘められた魔力で%CALLNAME:PLAYER%の精力が回復した…
			BASE:PLAYER:精力 = MIN(BASE:PLAYER:精力 + 2, MAXBASE:PLAYER:精力)
		ENDIF
	ENDSELECT
	RETURN 1
ENDIF


PRINTFORM 宝箱を見つけた。
;トラップ発動。通常は20％。罠解除持ちだと５％。誘発持ちだと50％
IF (ASSI <= 0 && RAND:5 == 0) || (ASSI && RAND:(5 + COND("探索：罠解除", ASSI) - COND("探索：罠誘発", ASSI) ) == 0)
	PRINT トラップ
	IF RAND:5 == 0
		PRINTFORML ？リャナンシーの罠だ！…%CALLNAME:PLAYER%に幸運が舞い降りた！
		CALL SET_PALAM_EXPLORER, "幸運", RAND:3 + 2
		CALL SET_PALAM_EXPLORER, "アイテム発見率", 50
		CALL SET_PALAM_EXPLORER, "アイテムレベル", 3
	ELSEIF PALAM_EXPLORER("アイテムレベル") >= 5 && RAND:6 == 0
		PRINTFORML ！リリスの罠だ！%CALLNAME:PLAYER%は……とても気持ちよかった。
		BASE:PLAYER:精力 -= RAND:6 + 5
	ELSEIF PALAM_EXPLORER("アイテムレベル") >= 3 && RAND:5 == 0
		PRINTFORML ！エスケプの罠だ！%CALLNAME:PLAYER%は少し遠くへ飛ばされてしまった！
		COUNT:99 += RAND:5 + 1
	ELSEIF RAND:4 == 0
		PRINTFORML ！毒の罠だ！%CALLNAME:PLAYER%は感じやすくなってしまった！
		IF COND("探索：状態異常無効持ちが居る")
			PRINTFORML …が、すぐさま治療した。
		ELSE
			CALL SET_PALAM_EXPLORER, "高敏术", RAND:4 + 3
		ENDIF
	ELSEIF RAND:3 == 0
		PRINTFORML ！アルカナの罠だ！%CALLNAME:PLAYER%はひどく不器用になってしまった！
		IF COND("探索：状態異常無効持ちが居る")
			PRINTFORML …が、すぐさま治療した。
		ELSE
			CALL SET_PALAM_EXPLORER, "クラムジ", RAND:4 + 3
		ENDIF
	ELSEIF RAND:2 == 0
		PRINTFORML ！ハーレムの罠だ！夢魔が一斉に向かってくる！
		LOCAL = RAND:4 + 3
		CALL SET_PALAM_EXPLORER, "ハーレム", LOCAL
		CALL SET_PALAM_EXPLORER, "遭遇率", LOCAL*10
	ELSE
		PRINTFORML ！サキュバスの罠だ！%CALLNAME:PLAYER%は……気持ちよかった。
		BASE:PLAYER:精力 -= RAND:6 + 1
	ENDIF
	RETURN 0
ENDIF

CALL TREASURE_BOX_EXPLORER_B



;-------------------------------------------------
;宝箱の中身決定関数
;ARGは戦闘戦利品のときのみ、相手のTARGETが代入される
;-------------------------------------------------
@TREASURE_BOX_EXPLORER_B, ARG
#DIM LCOUNT
VARSET LOCAL
;アイテムを持てるかチェック
FOR LCOUNT, 0, 50
	SIF LCOUNT <= 29 && ITEM:LCOUNT
		CONTINUE
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 96, 100
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
FOR LCOUNT, 300, 330
	SIF ITEM:LCOUNT + I:LCOUNT < 99
		LOCAL:LCOUNT = 1
NEXT
SIF ITEM:淫魔水滴 + I:350 < 999
	LOCAL:350 = 1

SELECTCASE RAND:5
CASE 0
	IF LOCAL:99 && RAND:3
		PRINTFORM 中には
		CALL PRINT_COLORTEXT, "月之糖果", DEF_COLOR("黄色")
		PRINTFORML が入っていた。
		IF PLAYER != MASTER && COND("夢魔", PLAYER)
			PRINTFORM %CALLNAME:PLAYER%はご満悦だ。
			CFLAG:PLAYER:2 += 50
			BASE:PLAYER:精力 = MAXBASE:PLAYER:精力
		ELSE
			I:99 += 1
		ENDIF
		RETURN 0
	ELSEIF LOCAL:312 && RANDIF(1, LOCAL:96, LOCAL:311)
		PRINTFORM 中から
		CALL PRINT_COLORTEXT, "紋白蝶", DEF_COLOR("黄色")
		PRINTFORML がヒラヒラと飛び出してきた。
		I:312 += 1
		RETURN 0
	ELSEIF LOCAL:311 && RANDIF(1, LOCAL:96)
		PRINTFORM 中には
		CALL PRINT_COLORTEXT, "灼烧思考之炎", DEF_COLOR("黄色")
		PRINTFORML が入っていた。
		I:311 += 1
		RETURN 0
	ELSEIF LOCAL:96
		PRINTFORM 中には
		CALL PRINT_COLORTEXT, "砥砺思维之石", DEF_COLOR("黄色")
		PRINTFORML が入っていた。
		I:96 += 1
		RETURN 0
	ENDIF
;アクセサリ
CASE 1
	PRINTFORM 中には

	;特殊なアクセサリ
	IF ARG > 0
		SELECTCASE CALLNAME:ARG
		CASE "ヴィーヴル"
			CALL PRINT_COLORTEXT, "朦胧宝石", DEF_COLOR("黄色")
			I:325 += 1
			PRINTFORML が入っていた。
			RETURN 0
		ENDSELECT
	ENDIF

	SELECTCASE RAND:9
	CASE 0 TO 1
		CALL PRINT_COLORTEXT, "朦胧手镯", DEF_COLOR("黄色")
		I:320 += 1
	CASE 2 TO 3
		CALL PRINT_COLORTEXT, "朦胧袖饰", DEF_COLOR("黄色")
		I:321 += 1
	CASE 4 TO 5
		CALL PRINT_COLORTEXT, "朦胧手套", DEF_COLOR("黄色")
		I:322 += 1
	CASE 6 TO 7
		CALL PRINT_COLORTEXT, "朦胧绶带", DEF_COLOR("黄色")
		I:323 += 1
	CASEELSE
		CALL PRINT_COLORTEXT, "朦胧指环", DEF_COLOR("黄色")
		I:324 += 1
	ENDSELECT
	PRINTFORML が入っていた。
	RETURN 0
ENDSELECT

LOCAL:101 = 1
FOR LOCAL:102, 0, PALAM_EXPLORER("アイテムレベル")+1
	SIF PALAM_EXPLORER("アイテム発見率") > RAND:100
		LOCAL:101 += 1
NEXT

CALL SET_PALAM_EXPLORER, "財宝", RAND:(LOCAL:101)

MONEY:5 = 0
;ローズニルの詩集
IF PALAM_EXPLORER("財宝") <= 3 && RAND:(3 + (ITEM:罗兹尼尔诗集 + I:310 > 0)*7) == 0 && LOCAL:315
	CALL PRINT_COLORTEXT, "ローズニルの詩集", DEF_COLOR("黄色")
	PRINTFORML を手に入れました。
	I:310 += 1
	RETURN 0
ENDIF

SELECTCASE PALAM_EXPLORER("財宝")
;お金
CASE 0
	MONEY:5 = PALAM_EXPLORER("財宝期待値")*PALAM_EXPLORER("アイテムレベル")*3 + RAND:100
;LV1 安いアイテム($500,装備品は半額程度)
CASE 1
	IF RAND:10 == 0 && LOCAL:300
		CALL PRINT_COLORTEXT, "陶磁器のスプーン", DEF_COLOR("黄色")
		I:300 += 1
	ELSEIF LOCAL:30 && RANDIF(2, LOCAL:40, LOCAL:32)
		CALL PRINT_COLORTEXT, "ローション", DEF_COLOR("黄色")
		I:30 += 1
	ELSEIF LOCAL:32 && RANDIF(2, LOCAL:40)
		CALL PRINT_COLORTEXT, "利尿剤", DEF_COLOR("黄色")
		I:32 += 1
	ELSEIF LOCAL:40 && RAND:2
		CALL PRINT_COLORTEXT, "茉莉薰香", DEF_COLOR("黄色")
		I:40 += 1
	ELSE
		MONEY:5 = 250 + RAND:100
	ENDIF
;LV2 安いアイテム2($1000)
CASE 2
	IF RAND:5 == 0 && LOCAL:300
		CALL PRINT_COLORTEXT, "陶磁器のスプーン", DEF_COLOR("黄色")
		I:300 += 1
	ELSEIF LOCAL:1 && RANDIF(2, LOCAL:41, LOCAL:33, LOCAL:0, LOCAL:10)
		CALL PRINT_COLORTEXT, "バイブ", DEF_COLOR("黄色")
		I:1 += 1
	ELSEIF LOCAL:10 && RANDIF(2, LOCAL:41, LOCAL:33, LOCAL:0)
		CALL PRINT_COLORTEXT, "鞭", DEF_COLOR("黄色")
		I:10 += 1
	ELSEIF LOCAL:0 && RANDIF(2, LOCAL:41, LOCAL:33)
		CALL PRINT_COLORTEXT, "ローター", DEF_COLOR("黄色")
		I:0 += 1
	ELSEIF LOCAL:33 && RANDIF(2, LOCAL:41, LOCAL:33)
		CALL PRINT_COLORTEXT, "ビデオテープ", DEF_COLOR("黄色")
		I:33 += 1
	ELSEIF LOCAL:41 && RAND:2
		CALL PRINT_COLORTEXT, "薰衣草薰香", DEF_COLOR("黄色")
		I:41 += 1
	ELSE
		MONEY:5 = 400 + RAND:200
	ENDIF
;LV3 程ほどの値段のアイテム($2000)
CASE 3
	IF RAND:5 == 0 && LOCAL:301
		CALL PRINT_COLORTEXT, "陶磁器のティーカップ", DEF_COLOR("黄色")
		I:301 += 1
	ELSEIF LOCAL:9 && RANDIF(2, LOCAL:43, LOCAL:31)
		CALL PRINT_COLORTEXT, "口枷セット", DEF_COLOR("黄色")
		I:9 += 1
	ELSEIF LOCAL:31 && RANDIF(2, LOCAL:43)
		CALL PRINT_COLORTEXT, "媚薬", DEF_COLOR("黄色")
		I:31 += 1
	ELSEIF LOCAL:43 && RAND:2
		CALL PRINT_COLORTEXT, "檀香薰香", DEF_COLOR("黄色")
		I:43 += 1
	ELSE
		MONEY:5 = 800 + RAND:500
	ENDIF
;LV4 程ほどの値段のアイテム2($4000)
CASE 4
	IF RAND:10 == 0 && LOCAL:305
		CALL PRINT_COLORTEXT, "銀のスプーン", DEF_COLOR("黄色")
		I:302 += 1
	ELSEIF RAND:5 == 0 && LOCAL:301
		CALL PRINT_COLORTEXT, "陶磁器のティーカップ", DEF_COLOR("黄色")
		I:301 += 1
	ELSEIF LOCAL:4 && RANDIF(2, LOCAL:34, LOCAL:44, LOCAL:17)
		CALL PRINT_COLORTEXT, "ペニスバンド", DEF_COLOR("黄色")
		I:4 += 1
	ELSEIF LOCAL:17 && RANDIF(2, LOCAL:34, LOCAL:44)
		CALL PRINT_COLORTEXT, "搾乳機", DEF_COLOR("黄色")
		I:17 += 1
	ELSEIF LOCAL:44 && RAND:2
		CALL PRINT_COLORTEXT, "迷迭香薰香", DEF_COLOR("黄色")
		I:44 += 1
	ELSEIF LOCAL:34 && RAND:2
		CALL PRINT_COLORTEXT, "鎮静剤", DEF_COLOR("黄色")
		I:34 += 1
	ELSE
		MONEY:5 = 1700 + RAND:1000
	ENDIF
;LV5 程ほどの値段のアイテム3($6000)
CASE 5
	IF RAND:5 == 0 && LOCAL:305
		CALL PRINT_COLORTEXT, "銀のスプーン", DEF_COLOR("黄色")
		I:302 += 1
	ELSEIF LOCAL:8 && RANDIF(2, LOCAL:42, LOCAL:14, LOCAL:22)
		CALL PRINT_COLORTEXT, "ニプルキャップ", DEF_COLOR("黄色")
		I:8 += 1
	ELSEIF LOCAL:22 && RANDIF(2, LOCAL:42, LOCAL:14)
		CALL PRINT_COLORTEXT, "空気ポンプ", DEF_COLOR("黄色")
		I:22 += 1
	ELSEIF LOCAL:14 && RANDIF(2, LOCAL:42)
		CALL PRINT_COLORTEXT, "拘束ツール", DEF_COLOR("黄色")
		I:14 += 1
	ELSEIF LOCAL:42 && RAND:2
		CALL PRINT_COLORTEXT, "麝香薰香", DEF_COLOR("黄色")
		I:42 += 1
	ELSE
		MONEY:5 = 2500 + RAND:1000
	ENDIF
;LV6 高価なアイテム($8000)
CASE 6
	IF RAND:10 == 0 && LOCAL:306
		CALL PRINT_COLORTEXT, "銀のティーカップ", DEF_COLOR("黄色")
		I:303 += 1
	ELSEIF LOCAL:5 && RANDIF(2, LOCAL:49, LOCAL:12)
		CALL PRINT_COLORTEXT, "アイマスク", DEF_COLOR("黄色")
		I:5 += 1
	ELSEIF LOCAL:12 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "オナホール", DEF_COLOR("黄色")
		I:12 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "乳香", DEF_COLOR("黄色")
		I:49 += 1
	ELSE
		MONEY:5 = 3500 + RAND:1000
	ENDIF
;LV7 高価なアイテム2($10000)
CASE 7
	IF RAND:5 == 0 && LOCAL:306
		CALL PRINT_COLORTEXT, "銀のティーカップ", DEF_COLOR("黄色")
		I:303 += 1
	ELSEIF LOCAL:2 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18, LOCAL:13, LOCAL:3)
		CALL PRINT_COLORTEXT, "Ｅマッサージャ", DEF_COLOR("黄色")
		I:2 += 1
	ELSEIF LOCAL:3 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18, LOCAL:13)
		CALL PRINT_COLORTEXT, "アナルバイブ", DEF_COLOR("黄色")
		I:3 += 1
	ELSEIF LOCAL:13 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25, LOCAL:18)
		CALL PRINT_COLORTEXT, "プレイマット", DEF_COLOR("黄色")
		I:13 += 1
	ELSEIF LOCAL:18 && RANDIF(2, LOCAL:49, LOCAL:26, LOCAL:25)
		CALL PRINT_COLORTEXT, "首輪", DEF_COLOR("黄色")
		I:18 += 1
	ELSEIF LOCAL:25 && RANDIF(2, LOCAL:49, LOCAL:26)
		CALL PRINT_COLORTEXT, "アナルパール", DEF_COLOR("黄色")
		I:25 += 1
	ELSEIF LOCAL:26 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "魔法瓶", DEF_COLOR("黄色")
		I:26 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "乳香", DEF_COLOR("黄色")
		I:49 += 1
	ELSE
		MONEY:5 = 4500 + RAND:1000
	ENDIF
;LV8 レアアイテム($16000)
CASEELSE
	IF RAND:10 == 0 && LOCAL:311
		CALL PRINT_COLORTEXT, "ミスリルのティーカップ", DEF_COLOR("黄色")
		I:305 += 1
	ELSEIF RAND:5 == 0 && LOCAL:310
		CALL PRINT_COLORTEXT, "ミスリルのスプーン", DEF_COLOR("黄色")
		I:304 += 1
	ELSEIF CONFIG("妊娠パッチ") && LOCAL:35 && RANDIF(2, LOCAL:49, LOCAL:6, LOCAL:15, LOCAL:23)
		CALL PRINT_COLORTEXT, "排卵誘発剤", DEF_COLOR("黄色")
		I:35 += 1
	ELSEIF LOCAL:23 && RANDIF(2, LOCAL:49, LOCAL:6, LOCAL:15)
		CALL PRINT_COLORTEXT, "拡張バルーン", DEF_COLOR("黄色")
		I:23 += 1
	ELSEIF LOCAL:15 && RANDIF(2, LOCAL:49, LOCAL:6)
		CALL PRINT_COLORTEXT, "浣腸器＋プラグ", DEF_COLOR("黄色")
		I:15 += 1
	ELSEIF LOCAL:6 && RANDIF(2, LOCAL:49)
		CALL PRINT_COLORTEXT, "ビデオカメラ", DEF_COLOR("黄色")
		I:6 += 1
	ELSEIF LOCAL:49 && RAND:2
		CALL PRINT_COLORTEXT, "乳香", DEF_COLOR("黄色")
		I:49 += 1
	ELSE
		MONEY:5 = 7500 + RAND:1000
	ENDIF
ENDSELECT
IF MONEY:5
	CALL PRINT_COLORTEXT, MONEY_C(MONEY:5), DEF_COLOR("黄色")
	PRINTFORML を得ました。
	MONEY:6 += MONEY:5
ELSE
	PRINTFORML を手に入れました。
ENDIF
