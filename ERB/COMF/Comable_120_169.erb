;-------------------------------------------------
;120番台,調教者セックス系コマンド
;-------------------------------------------------
@COM_ABLE120
;正常位奉仕
SIF COMABLE_DEF(120)
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:オトコ
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
IF TEQUIP:押し倒し
	;サドっ気が必要
	SIF ABL:サドっ気 == 0
		RETURN 0
	SIF TALENT:PLAYER:処女 && ABL:サドっ気 < 3
		RETURN 0
ELSE
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM120, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE121
;後背位奉仕
SIF COMABLE_DEF(121)
	RETURN 0
SIF TEQUIP:押し倒し
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:オトコ
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM121, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE122
;騎乗位で犯す
SIF COMABLE_DEF(122)
	RETURN 0
SIF TEQUIP:押し倒し
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:オトコ
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
SIF ABL:PLAYER:サドっ気 == 0 || ABL:PLAYER:Ｖ拡張 < SIZE("ペニス", TARGET) || EXP:PLAYER:Ｖ経験 < 50 || EXP:PLAYER:Ｖ性交経験 < 20
	RETURN 0
RETURN 1

@COM_ABLE123
RETURN 0

@COM_ABLE124
RETURN 0

@COM_ABLE125
;Ａ正常位奉仕実行判定
SIF COMABLE_DEF(125)
	RETURN 0
SIF TEQUIP:押し倒し
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM125, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE126
;Ａ後背位奉仕実行判定
SIF COMABLE_DEF(126)
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
IF TEQUIP:押し倒し
	;サドっ気が必要
	SIF ABL:サドっ気 == 0
		RETURN 0
	SIF TALENT:PLAYER:Ａ処女 && ABL:サドっ気 < 3
		RETURN 0
ELSE
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM126, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE127
;Ａ騎乗位で犯す実行判定
SIF COMABLE_DEF(127)
	RETURN 0
SIF TEQUIP:押し倒し
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
SIF ABL:PLAYER:サドっ気 == 0 || ABL:PLAYER:Ａ拡張 < SIZE("ペニス", TARGET) || EXP:PLAYER:Ａ経験 < 50 || EXP:PLAYER:Ａ性交経験 < 20
	RETURN 0
RETURN 1

@COM_ABLE128
RETURN 0

@COM_ABLE129
RETURN 0


;-------------------------------------------------
;130〜159番,SM系orハードなコマンド
;-------------------------------------------------
@COM_ABLE130
;スパンキング実行判定
SIF COMABLE_DEF(130)
	RETURN 0
RETURN 1

@COM_ABLE131
;鞭実行判定
SIF COMABLE_DEF(131)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:鞭 == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE132
;針実行判定
SIF COMABLE_DEF(132)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:針 == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE133
;アイマスク実行判定
SIF COMABLE_DEF(133)
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:アイマスク
	RETURN 1
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:アイマスク == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE134
;縄実行判定
SIF COMABLE_DEF(134)
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:拘束
	RETURN 1
;アイテムを持っているか、アイテムなし設定で無いとダメ
SIF ITEM:拘束ツール == 0 && NOITEM == 0
	RETURN 0
;助手は技巧5でないとダメ
IF ASSIPLAY
	SIF ABL:ASSI:技巧 <= 4
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE135
;口枷実行判定
SIF COMABLE_DEF(135)
	RETURN 0
;触手口辱中だとダメ
SIF TEQUIP:触手口辱
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:口枷
	RETURN 1
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:口枷セット == 0 && NOITEM == 0
	RETURN 0
;ASSIの場合、技巧3以上じゃないと使えない。
IF ASSIPLAY
	SIF ABL:ASSI:技巧 < 3
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE136
;浣腸実行判定
SIF COMABLE_DEF(136)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:浣腸器＋プラグ == 0 && NOITEM == 0
	RETURN 0
;解除、追加はいつでも可能
IF TEQUIP:浣腸 && COND("アナル栓")
	SIF COND("精液浣腸")
		RETURN 0
	RETURN 1
ELSE
	SIF COND("空気浣腸") || COND("液体浣腸") >= 4
		RETURN 0
ENDIF
;アナル使用判定
SIF USE_A(TARGET, 46) == 0
	RETURN 0
RETURN 1

@COM_ABLE137
;拡張バルーン実行判定
SIF COMABLE_DEF(137)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:拡張バルーン == 0 && NOITEM == 0
	RETURN 0
LOCAL = 0
;アソコ＆アナル使用判定
SIF USE_V(TARGET, 48) && TALENT:処女 == 0 && TALENT:Ｖ損傷 == 0 && ABL:Ｖ拡張
	LOCAL = 1
SIF USE_A(TARGET, 48) && TALENT:Ａ損傷 == 0 && ABL:Ａ拡張 && !(FLAG:209&2)
	LOCAL = 1
SIF LOCAL == 0
	RETURN 0
RETURN 1

@COM_ABLE138
;浴尿させる実行判定（とはいえ今のところは口か膣か腸内への放尿のみ）
SIF COMABLE_DEF(138)
	RETURN 0
;プレイヤーのサドっ気が3LV必須
SIF ABL:PLAYER:サドっ気 < 3 && TALENT:PLAYER:サド == 0
	RETURN 0
;尿意が溜まってる？
SIF BASE:PLAYER:尿意 < 10000
	RETURN 0
;膣内放尿が可能かつ前回子宮挿入なら子宮内放尿になる
SIF CHECK_COM("口内放尿", 138) || CHECK_COM("膣内放尿", 138) || CHECK_COM("温泉浣腸", 138) || CHECK_COM("乳内放尿", 138) || CHECK_COM("膀胱内放尿", 138)
	RETURN 1
RETURN 0

@COM_ABLE139
;何もしない実行判定
SIF COMABLE_DEF(139)
	RETURN 0
;主人以外は不可にすべきかどうか
;SIF PLAYER != MASTER
;	RETURN 0
;連続実行は無理
SIF PREVCOM == 139
	RETURN 0
;３Ｐ中はとりあえず封印
SIF COND("３Ｐ")
	RETURN 0
;ビデオ中も封印
SIF TEQUIP:ビデオカメラ
	RETURN 0
RETURN 1

@COM_ABLE140
;イラマチオ実行判定
SIF COMABLE_DEF(140)
	RETURN 0
;調教者がオトコかふたなりorペニバンがないとダメ
SIF PENIS(PLAYER) == 0 && COND("ペニバン", PLAYER) == 0
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口枷 == 1
	RETURN 0
;調教者がサドっ気あり、従順+欲望が6以上、リングギャグ装着、喉LVが高いでないとダメ
SIF ABL:PLAYER:サドっ気 == 0 && ABL:従順 + ABL:欲望 < 6 && TEQUIP:口枷 != 2 && CHECK("喉姦") < 2
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM140, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE141
;フィストファック
SIF COMABLE_DEF(141)
	RETURN 0
;Ｖ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("片手フィスト", PLAYER) - ABL:Ｖ拡張 - TALENT:PLAYER:サド * 2 >= 2
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:お風呂プレイ
	SIF ITEM:プレイマット == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE142
;アナルフィスト
SIF COMABLE_DEF(142)
	RETURN 0
;Ａ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("片手フィスト", PLAYER) - ABL:Ａ拡張 - TALENT:PLAYER:サド * 2 >= 2
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:お風呂プレイ
	SIF ITEM:プレイマット == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE143
;両穴フィスト
SIF COMABLE_DEF(143)
	RETURN 0
;ＶＡフィストが出来ないとダメ
CALL COM_ABLE141
SIF RESULT == 0
	RETURN 0
CALL COM_ABLE142
SIF RESULT == 0
	RETURN 0
;妖精だとダメ
SIF TALENT:妖精
	RETURN 0
;Ｖ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("Ｗフィスト", PLAYER) - ABL:Ｖ拡張 - TALENT:PLAYER:サド * 2 >= 2
	RETURN 0
;Ａ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("Ｗフィスト", PLAYER) - ABL:Ａ拡張 - TALENT:PLAYER:サド * 2 >= 2
	RETURN 0
RETURN 1

@COM_ABLE144
;放尿実行判定
SIF COMABLE_DEF(144)
	RETURN 0
;尿意ゲージが5000以上でないとダメ
SIF BASE:尿意 < 5000
	RETURN 0
;Ｖ拡張バルーン
SIF TEQUIP:拡張バルーン == 1
	RETURN 0
;相手がペニス持ちだと強制放尿は無理
SIF PENIS(TARGET) && CHECK_COM("強制放尿", 144)
	RETURN 0
;押し倒しの時には聖水プレイになる（とりあえずオンナのみ）
IF TEQUIP:押し倒し
	SIF TEQUIP:顔面騎乗 == 0 || PENIS(TARGET) || TEQUIP:Ｗ押し倒し
		RETURN 0
	SIF ABL:サドっ気 == 0 || ABL:露出癖 == 0 || ABL:欲望 < 3
		RETURN 0
	SIF ABL:サドっ気 < 2 && TALENT:倒錯的 == 0
		RETURN 0
	SIF ABL:サドっ気 + ABL:PLAYER:マゾっ気 < 5
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM144, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE145
;剃毛実行判定
SIF COMABLE_DEF(145)
	RETURN 0
;押し倒している時or女王様プレイの時にはPLAYERの陰毛を剃ります！
IF TEQUIP:押し倒し || TEQUIP:女王様プレイ
	;バイブ、クリキャップはダメ
	SIF TEQUIP:PLAYER:バイブ || TEQUIP:PLAYER:クリキャップ
		RETURN 0
	;技巧2LV、サドっ気 >= 3、PLAYERに毛が生えていないとダメ
	SIF COND("陰毛", PLAYER) <= 3 || ABL:技巧 < 2 || ABL:サドっ気 + ABL:PLAYER:マゾっ気 < 5
		RETURN 0
ELSE
	;バイブ、クリキャップはダメ
	SIF TEQUIP:バイブ || TEQUIP:クリキャップ
		RETURN 0
	;毛が生えていないとダメ
	SIF COND("陰毛") <= 3
		RETURN 0
	;アソコ剥き出しにできないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM145, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE146
;各調教で一度だけ
SIF CHOSECOM(146)
	RETURN 0
;身嗜み管理実行判定
SIF COMABLE_DEF(146)
	RETURN 0
;何か指示できる状態でないとダメ
IF TALENT:オトコ == 0 || PENIS(TARGET) == 0
ELSEIF ABL:従順 >= 3 || TALENT:恋慕 || MARK:屈服刻印 >= 2
ELSEIF CONFIG("陰毛") && COND("陰毛") >= 2
ELSEIF CONFIG("腋毛") && COND("腋毛") >= 2
ELSEIF ABL:Ｖ拡張 >= 1 && TALENT:オトコ == 0
ELSEIF ABL:Ａ拡張 >= 1
ELSEIF ABL:Ａ拡張 >= 1 && (EXP:浣腸経験 || EXP:Ａ経験 >= 100 || EXP:Ａ性交経験 >= 20)
ELSEIF ABL:尿道拡張 >= 0 && EXP:尿道経験 >= 5 && (EXP:尿道経験 >= 20 || EXP:尿道拡張経験 >= 5 || ABL:尿道拡張 >= 1)
ELSEIF PENIS(TARGET) && COND("真性包茎") == 0
ELSE
	RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM146, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE147
;精飲実行判定
SIF COMABLE_DEF(147)
	RETURN 0
;ボールギャグしているとダメ
SIF TEQUIP:口枷 == 1
	RETURN 0
;精液がたまっていないとダメ
SIF ITEM:精液貯蓄量 == 0
	RETURN 0
;ある程度興奮していないとダメ
SIF PALAM:欲情 < 3000
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM147, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE148
;ピアッシング実行判定
SIF COMABLE_DEF(148)
	RETURN 0
;アイテムがないとダメ
SIF ITEM:ピアスセット == 0
	RETURN 0
;PLAYERがサド、針さばき、狂気、TARGETが淫乱、倒錯的、マゾ、狂気、露出癖、マゾっ気等の総合値で正否を判断
SIF TALENT:PLAYER:サド*10 + TALENT:PLAYER:針さばき*2 + TALENT:PLAYER:狂気 + TALENT:淫乱*5 + TALENT:倒錯的*5 + TALENT:マゾ*7 + TALENT:狂気 + ABL:露出癖 + ABL:マゾっ気*2 < 10
	RETURN 0
RETURN 1

@COM_ABLE149
;カテーテル実行判定
SIF COMABLE_DEF(149)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:尿道責めキット == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:オナホール
	RETURN 0
SIF TEQUIP:尿道ブジー
	RETURN 0
SIF TEQUIP:尿道バイブ
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM149, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE150
;ディープスロート実行判定
SIF COMABLE_DEF(150)
	RETURN 0
;解除させるのはいつでも
SIF TEQUIP:ディープスロート
	RETURN 1
;調教者にちんこが無いとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口枷 == 1
	RETURN 0
;ペニスが小さいとダメ
SIF SIZE("ペニス", PLAYER) == 0
	RETURN 0
;喉姦可能で無いとダメ（今後変えるかも）
SIF CHECK("喉姦") == 0
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM150, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE151
;尿道ブジー実行判定
SIF COMABLE_DEF(151)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:尿道責めキット == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:オナホール
	RETURN 0
;カテーテル使用中はダメ
SIF TEQUIP:カテーテル
	RETURN 0
SIF TEQUIP:尿道バイブ
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM151, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE152
;尿道バイブ実行判定
SIF COMABLE_DEF(152)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:尿道責めキット == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:オナホール
	RETURN 0
;カテーテル使用中はダメ
SIF TEQUIP:カテーテル
	RETURN 0
SIF TEQUIP:尿道ブジー
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM152, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1
@COM_ABLE153
RETURN 0

@COM_ABLE154
RETURN 0

;-------------------------------------------------
;160番台,助手による汎用コマンド
;-------------------------------------------------
@COM_ABLE160
;助手を犯す実行判定
SIF COMABLE_DEF(160)
	RETURN 0
;館内露出プレイ中はダメ
SIF TEQUIP:館内露出プレイ
	RETURN 0
;野外プレイ中はダメ
SIF TEQUIP:野外プレイ
	RETURN 0
;主人じゃないとダメ
SIF ASSIPLAY
	RETURN 0
;助手がオトコだとダメ
SIF TALENT:ASSI:オトコ
	RETURN 0
;主人がオトコ、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(MASTER) == 0 && COND("ペニバン", MASTER) == 0
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:お風呂プレイ
	SIF ITEM:プレイマット == 0 && NOITEM == 0
		RETURN 0
ENDIF
;対象と助手の相性が良くないとダメ
SIF RELATION:(NO:ASSI) < 120
	RETURN 0
;ピクシーが助手の時はミニマムでないとダメ
IF TALENT:ASSI:妖精
	SIF ABL:ASSI:Ｖ拡張 < SIZE("ペニス", MASTER)
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE161
;助手を犯させる実行判定
SIF COMABLE_DEF(161)
	RETURN 0
;助手じゃないとダメ
SIF PLAYER != ASSI
	RETURN 0
;助手がオトコだとダメ
SIF TALENT:PLAYER:オトコ
	RETURN 0
;オトコ、半人半妖、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(TARGET) == 0 && COND("ペニバン") == 0
	RETURN 0
;オナホール装着中はダメ
SIF TEQUIP:オナホール
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:お風呂プレイ
	SIF ITEM:プレイマット == 0 && NOITEM == 0
		RETURN 0
ENDIF
;ピクシーが助手の時はダメ
SIF TALENT:ASSI:妖精
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM161, 1
	RETURN LIMIT(RESULT, 0, 1)

ENDIF
RETURN 1

@COM_ABLE162
;助手フェラ実行判定
SIF COMABLE_DEF(162)
	RETURN 0
;館内露出プレイ中はダメ
SIF TEQUIP:館内露出プレイ
	RETURN 0
;野外プレイ中はダメ
SIF TEQUIP:野外プレイ
	RETURN 0
;助手が歌っているとダメ
SIF TEQUIP:ASSI:演奏中
	RETURN 0
;主人じゃないとダメ
SIF ASSIPLAY
	RETURN 0
;主人がオトコ、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(MASTER) == 0
	RETURN 0
;汚臭敏感でアナルの汚れがある時には、恋慕か従順+Ａ感覚+奉仕精神+レズっ気＞12が必要
SIF TALENT:ASSI:汚臭敏感 && CHECK_STAIN(STAIN:PLAYER:Ｐ, "Ａ") && TALENT:ASSI:恋慕 == 0 && ABL:ASSI:従順+ABL:ASSI:Ａ感覚+ABL:ASSI:奉仕精神+ABL:ASSI:レズっ気 < 13
	RETURN 0
RETURN 1

@COM_ABLE163
RETURN 0

@COM_ABLE164
RETURN 0
