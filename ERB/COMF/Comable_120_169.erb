;-------------------------------------------------
;120番台,調教者セックス系コマンド
;-------------------------------------------------
@COM_ABLE120
;正常位奉仕
SIF COMABLE_DEF(120)
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
IF TEQUIP:推到
	;サドっ気が必要
	SIF ABL:施虐倾向 == 0
		RETURN 0
	SIF TALENT:PLAYER:处女 && ABL:施虐倾向 < 3
		RETURN 0
ELSE
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM120, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE121
;後背位奉仕
SIF COMABLE_DEF(121)
	RETURN 0
SIF TEQUIP:推到
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM121, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE122
;騎乗位で犯す
SIF COMABLE_DEF(122)
	RETURN 0
SIF TEQUIP:推到
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
SIF ABL:PLAYER:施虐倾向 == 0 || ABL:PLAYER:Ｖ扩张 < SIZE("ペニス", TARGET) || EXP:PLAYER:Ｖ经验 < 50 || EXP:PLAYER:Ｖ性交经验 < 20
	RETURN 0
RETURN 1

@COM_ABLE123
RETURN 0

@COM_ABLE124
RETURN 0

@COM_ABLE125
;Ａ正常位奉仕実行判定
SIF COMABLE_DEF(125)
	RETURN 0
SIF TEQUIP:推到
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM125, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE126
;Ａ後背位奉仕実行判定
SIF COMABLE_DEF(126)
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
IF TEQUIP:推到
	;サドっ気が必要
	SIF ABL:施虐倾向 == 0
		RETURN 0
	SIF TALENT:PLAYER:Ａ处女 && ABL:施虐倾向 < 3
		RETURN 0
ELSE
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM126, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE127
;Ａ騎乗位で犯す実行判定
SIF COMABLE_DEF(127)
	RETURN 0
SIF TEQUIP:推到
	RETURN 0
;ペニスあり
IF PENIS(TARGET)
	;ペニスを使う時には股間が露出できないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
;ペニスなし
ELSE
	;ペニスバンドがないとダメ
	SIF COND("ペニバン") == 0
		RETURN 0
ENDIF
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
SIF ABL:PLAYER:施虐倾向 == 0 || ABL:PLAYER:Ａ扩张 < SIZE("ペニス", TARGET) || EXP:PLAYER:Ａ经验 < 50 || EXP:PLAYER:Ａ性交经验 < 20
	RETURN 0
RETURN 1

@COM_ABLE128
RETURN 0

@COM_ABLE129
RETURN 0


;-------------------------------------------------
;130〜159番,SM系orハードなコマンド
;-------------------------------------------------
@COM_ABLE130
;スパンキング実行判定
SIF COMABLE_DEF(130)
	RETURN 0
RETURN 1

@COM_ABLE131
;鞭実行判定
SIF COMABLE_DEF(131)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:鞭 == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE132
;針実行判定
SIF COMABLE_DEF(132)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:针 == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE133
;アイマスク実行判定
SIF COMABLE_DEF(133)
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:眼罩
	RETURN 1
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:眼罩 == 0 && NOITEM == 0
	RETURN 0
RETURN 1

@COM_ABLE134
;縄実行判定
SIF COMABLE_DEF(134)
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:束缚
	RETURN 1
;アイテムを持っているか、アイテムなし設定で無いとダメ
SIF ITEM:束缚工具 == 0 && NOITEM == 0
	RETURN 0
;助手は技巧5でないとダメ
IF ASSIPLAY
	SIF ABL:ASSI:技巧 <= 4
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE135
;口枷実行判定
SIF COMABLE_DEF(135)
	RETURN 0
;触手口辱中だとダメ
SIF TEQUIP:触手口辱
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:口塞
	RETURN 1
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:口塞套件 == 0 && NOITEM == 0
	RETURN 0
;ASSIの場合、技巧3以上じゃないと使えない。
IF ASSIPLAY
	SIF ABL:ASSI:技巧 < 3
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE136
;浣腸実行判定
SIF COMABLE_DEF(136)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:灌肠器＋肛塞 == 0 && NOITEM == 0
	RETURN 0
;解除、追加はいつでも可能
IF TEQUIP:灌肠 && COND("アナル栓")
	SIF COND("精液浣腸")
		RETURN 0
	RETURN 1
ELSE
	SIF COND("空気浣腸") || COND("液体浣腸") >= 4
		RETURN 0
ENDIF
;アナル使用判定
SIF USE_A(TARGET, 46) == 0
	RETURN 0
RETURN 1

@COM_ABLE137
;拡張バルーン実行判定
SIF COMABLE_DEF(137)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:扩张气囊 == 0 && NOITEM == 0
	RETURN 0
LOCAL = 0
;アソコ＆アナル使用判定
SIF USE_V(TARGET, 48) && TALENT:处女 == 0 && TALENT:Ｖ损伤 == 0 && ABL:Ｖ扩张
	LOCAL = 1
SIF USE_A(TARGET, 48) && TALENT:Ａ损伤 == 0 && ABL:Ａ扩张 && !(FLAG:209&2)
	LOCAL = 1
SIF LOCAL == 0
	RETURN 0
RETURN 1

@COM_ABLE138
;浴尿させる実行判定（とはいえ今のところは口か膣か腸内への放尿のみ）
SIF COMABLE_DEF(138)
	RETURN 0
;プレイヤーのサドっ気が3LV必須
SIF ABL:PLAYER:施虐倾向 < 3 && TALENT:PLAYER:抖Ｓ == 0
	RETURN 0
;尿意が溜まってる？
SIF BASE:PLAYER:尿意 < 10000
	RETURN 0
;膣内放尿が可能かつ前回子宮挿入なら子宮内放尿になる
SIF CHECK_COM("口内放尿", 138) || CHECK_COM("膣内放尿", 138) || CHECK_COM("温泉浣腸", 138) || CHECK_COM("乳内放尿", 138) || CHECK_COM("膀胱内放尿", 138)
	RETURN 1
RETURN 0

@COM_ABLE139
;何もしない実行判定
SIF COMABLE_DEF(139)
	RETURN 0
;主人以外は不可にすべきかどうか
;SIF PLAYER != MASTER
;	RETURN 0
;連続実行は無理
SIF PREVCOM == 139
	RETURN 0
;３Ｐ中はとりあえず封印
SIF COND("３Ｐ")
	RETURN 0
;ビデオ中も封印
SIF TEQUIP:录像带
	RETURN 0
RETURN 1

@COM_ABLE140
;イラマチオ実行判定
SIF COMABLE_DEF(140)
	RETURN 0
;調教者がオトコかふたなりorペニバンがないとダメ
SIF PENIS(PLAYER) == 0 && COND("ペニバン", PLAYER) == 0
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞 == 1
	RETURN 0
;調教者がサドっ気あり、従順+欲望が6以上、リングギャグ装着、喉LVが高いでないとダメ
SIF ABL:PLAYER:施虐倾向 == 0 && ABL:服从 + ABL:欲望 < 6 && TEQUIP:口塞 != 2 && CHECK("喉姦") < 2
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM140, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE141
;フィストファック
SIF COMABLE_DEF(141)
	RETURN 0
;Ｖ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("片手フィスト", PLAYER) - ABL:Ｖ扩张 - TALENT:PLAYER:抖Ｓ * 2 >= 2
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE142
;アナルフィスト
SIF COMABLE_DEF(142)
	RETURN 0
;Ａ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("片手フィスト", PLAYER) - ABL:Ａ扩张 - TALENT:PLAYER:抖Ｓ * 2 >= 2
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE143
;両穴フィスト
SIF COMABLE_DEF(143)
	RETURN 0
;ＶＡフィストが出来ないとダメ
CALL COM_ABLE141
SIF RESULT == 0
	RETURN 0
CALL COM_ABLE142
SIF RESULT == 0
	RETURN 0
;妖精だとダメ
SIF TALENT:妖精
	RETURN 0
;Ｖ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("Ｗフィスト", PLAYER) - ABL:Ｖ扩张 - TALENT:PLAYER:抖Ｓ * 2 >= 2
	RETURN 0
;Ａ拡張LVが拳のサイズに匹敵していないと実行できない。サドは補正あり
SIF SIZE("Ｗフィスト", PLAYER) - ABL:Ａ扩张 - TALENT:PLAYER:抖Ｓ * 2 >= 2
	RETURN 0
RETURN 1

@COM_ABLE144
;放尿実行判定
SIF COMABLE_DEF(144)
	RETURN 0
;尿意ゲージが5000以上でないとダメ
SIF BASE:尿意 < 5000
	RETURN 0
;Ｖ拡張バルーン
SIF TEQUIP:扩张气囊 == 1
	RETURN 0
;相手がペニス持ちだと強制放尿は無理
SIF PENIS(TARGET) && CHECK_COM("強制放尿", 144)
	RETURN 0
;押し倒しの時には聖水プレイになる（とりあえずオンナのみ）
IF TEQUIP:推到
	SIF TEQUIP:颜面骑乘 == 0 || PENIS(TARGET) || TEQUIP:Ｗ推到
		RETURN 0
	SIF ABL:施虐倾向 == 0 || ABL:露出癖 == 0 || ABL:欲望 < 3
		RETURN 0
	SIF ABL:施虐倾向 < 2 && TALENT:性变态 == 0
		RETURN 0
	SIF ABL:施虐倾向 + ABL:PLAYER:受虐倾向 < 5
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM144, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE145
;剃毛実行判定
SIF COMABLE_DEF(145)
	RETURN 0
;押し倒している時or女王様プレイの時にはPLAYERの陰毛を剃ります！
IF TEQUIP:推到 || TEQUIP:女王大人Play
	;バイブ、クリキャップはダメ
	SIF TEQUIP:PLAYER:按摩棒 || TEQUIP:PLAYER:震动式阴蒂夹
		RETURN 0
	;技巧2LV、サドっ気 >= 3、PLAYERに毛が生えていないとダメ
	SIF COND("阴毛", PLAYER) <= 3 || ABL:技巧 < 2 || ABL:施虐倾向 + ABL:PLAYER:受虐倾向 < 5
		RETURN 0
ELSE
	;バイブ、クリキャップはダメ
	SIF TEQUIP:按摩棒 || TEQUIP:震动式阴蒂夹
		RETURN 0
	;毛が生えていないとダメ
	SIF COND("阴毛") <= 3
		RETURN 0
	;アソコ剥き出しにできないとダメ
	SIF !CHECK_CLO("Ｖ露出可")
		RETURN 0
	;実行値フィルタ
	IF CONFIG("実行値フィルタ") || FLAG:10
		CALL COM_ORDER_COM145, 1
		RETURN LIMIT(RESULT, 0, 1)
	ENDIF
ENDIF
RETURN 1

@COM_ABLE146
;各調教で一度だけ
SIF CHOSECOM(146)
	RETURN 0
;身嗜み管理実行判定
SIF COMABLE_DEF(146)
	RETURN 0
;何か指示できる状態でないとダメ
IF TALENT:男性 == 0 || PENIS(TARGET) == 0
ELSEIF ABL:服从 >= 3 || TALENT:恋慕 || MARK:屈服刻印 >= 2
ELSEIF CONFIG("阴毛") && COND("阴毛") >= 2
ELSEIF CONFIG("腋毛") && COND("腋毛") >= 2
ELSEIF ABL:Ｖ扩张 >= 1 && TALENT:男性 == 0
ELSEIF ABL:Ａ扩张 >= 1
ELSEIF ABL:Ａ扩张 >= 1 && (EXP:灌肠经验 || EXP:Ａ经验 >= 100 || EXP:Ａ性交经验 >= 20)
ELSEIF ABL:尿道扩张 >= 0 && EXP:尿道经验 >= 5 && (EXP:尿道经验 >= 20 || EXP:尿道扩张经验 >= 5 || ABL:尿道扩张 >= 1)
ELSEIF PENIS(TARGET) && COND("真性包茎") == 0
ELSE
	RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM146, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE147
;精飲実行判定
SIF COMABLE_DEF(147)
	RETURN 0
;ボールギャグしているとダメ
SIF TEQUIP:口塞 == 1
	RETURN 0
;精液がたまっていないとダメ
SIF ITEM:精液储存量 == 0
	RETURN 0
;ある程度興奮していないとダメ
SIF PALAM:欲情 < 3000
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM147, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE148
;ピアッシング実行判定
SIF COMABLE_DEF(148)
	RETURN 0
;アイテムがないとダメ
SIF ITEM:穿环套件 == 0
	RETURN 0
;PLAYERがサド、針さばき、狂気、TARGETが淫乱、倒錯的、マゾ、狂気、露出癖、マゾっ気等の総合値で正否を判断
SIF TALENT:PLAYER:抖Ｓ*10 + TALENT:PLAYER:针术*2 + TALENT:PLAYER:狂气 + TALENT:淫乱*5 + TALENT:性变态*5 + TALENT:抖Ｍ*7 + TALENT:狂气 + ABL:露出癖 + ABL:受虐倾向*2 < 10
	RETURN 0
RETURN 1

@COM_ABLE149
;カテーテル実行判定
SIF COMABLE_DEF(149)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:导尿管 == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:飞机杯
	RETURN 0
SIF TEQUIP:尿道扩张棒
	RETURN 0
SIF TEQUIP:尿道按摩棒
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM149, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE150
;ディープスロート実行判定
SIF COMABLE_DEF(150)
	RETURN 0
;解除させるのはいつでも
SIF TEQUIP:深喉
	RETURN 1
;調教者にちんこが無いとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞 == 1
	RETURN 0
;ペニスが小さいとダメ
SIF SIZE("ペニス", PLAYER) == 0
	RETURN 0
;喉姦可能で無いとダメ（今後変えるかも）
SIF CHECK("喉姦") == 0
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM150, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE151
;尿道ブジー実行判定
SIF COMABLE_DEF(151)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:导尿管 == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:飞机杯
	RETURN 0
;カテーテル使用中はダメ
SIF TEQUIP:导管
	RETURN 0
SIF TEQUIP:尿道按摩棒
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM151, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE152
;尿道バイブ実行判定
SIF COMABLE_DEF(152)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:导尿管 == 0 && NOITEM == 0
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可") || TEQUIP:飞机杯
	RETURN 0
;カテーテル使用中はダメ
SIF TEQUIP:导管
	RETURN 0
SIF TEQUIP:尿道扩张棒
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM152, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1
@COM_ABLE153
RETURN 0

@COM_ABLE154
RETURN 0

;-------------------------------------------------
;160番台,助手による汎用コマンド
;-------------------------------------------------
@COM_ABLE160
;助手を犯す実行判定
SIF COMABLE_DEF(160)
	RETURN 0
;館内露出プレイ中はダメ
SIF TEQUIP:馆内露出Play
	RETURN 0
;野外プレイ中はダメ
SIF TEQUIP:野外Play
	RETURN 0
;主人じゃないとダメ
SIF ASSIPLAY
	RETURN 0
;助手がオトコだとダメ
SIF TALENT:ASSI:男性
	RETURN 0
;主人がオトコ、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(MASTER) == 0 && COND("ペニバン", MASTER) == 0
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;対象と助手の相性が良くないとダメ
SIF RELATION:(NO:ASSI) < 120
	RETURN 0
;ピクシーが助手の時はミニマムでないとダメ
IF TALENT:ASSI:妖精
	SIF ABL:ASSI:Ｖ扩张 < SIZE("ペニス", MASTER)
		RETURN 0
ENDIF
RETURN 1

@COM_ABLE161
;助手を犯させる実行判定
SIF COMABLE_DEF(161)
	RETURN 0
;助手じゃないとダメ
SIF PLAYER != ASSI
	RETURN 0
;助手がオトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;オトコ、半人半妖、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(TARGET) == 0 && COND("ペニバン") == 0
	RETURN 0
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;ピクシーが助手の時はダメ
SIF TALENT:ASSI:妖精
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM161, 1
	RETURN LIMIT(RESULT, 0, 1)

ENDIF
RETURN 1

@COM_ABLE162
;助手フェラ実行判定
SIF COMABLE_DEF(162)
	RETURN 0
;館内露出プレイ中はダメ
SIF TEQUIP:馆内露出Play
	RETURN 0
;野外プレイ中はダメ
SIF TEQUIP:野外Play
	RETURN 0
;助手が歌っているとダメ
SIF TEQUIP:ASSI:演奏中
	RETURN 0
;主人じゃないとダメ
SIF ASSIPLAY
	RETURN 0
;主人がオトコ、ふたなり、もしくはペニスバンドがないとダメ
SIF PENIS(MASTER) == 0
	RETURN 0
;汚臭敏感でアナルの汚れがある時には、恋慕か従順+Ａ感覚+奉仕精神+レズっ気＞12が必要
SIF TALENT:ASSI:污臭敏感 && CHECK_STAIN(STAIN:PLAYER:Ｐ, "Ａ") && TALENT:ASSI:恋慕 == 0 && ABL:ASSI:服从+ABL:ASSI:Ａ感觉+ABL:ASSI:侍奉精神+ABL:ASSI:百合倾向 < 13
	RETURN 0
RETURN 1

@COM_ABLE163
RETURN 0

@COM_ABLE164
RETURN 0
