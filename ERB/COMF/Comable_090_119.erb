;-------------------------------------------------
;90〜119番,奉仕系コマンド
;-------------------------------------------------
@COM_ABLE90
;手淫実行判定
SIF COMABLE_DEF(90)
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM90, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE91
;フェラチオ実行判定
SIF COMABLE_DEF(91)
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞 == 1
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM91, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE92
;パイズリ実行判定
SIF COMABLE_DEF(92)
	RETURN 0
;胸を剥き出しにできるor胸露出でないとダメ
SIF CHECK_CLO("谷間露出") + CHECK_CLO("はだけ可") == 0
	RETURN 0
;オトコだとダメ
SIF TALENT:男性
	RETURN 0
;[巨乳]でなければ技巧2以上、[貧乳]だと技巧3以上必要
SIF (TALENT:贫乳 && ABL:技巧 < 3) || (TALENT:巨乳 == 0 && TALENT:贫乳 == 0 && ABL:技巧 < 2)
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM92, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE93
;素股実行判定
SIF COMABLE_DEF(93)
	RETURN 0
;アソコ剥き出しにできないとダメ
SIF !CHECK_CLO("Ｖ露出可")
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;オトコだとダメ
SIF TALENT:男性
	RETURN 0
;バイブ挿入中はダメ
SIF TEQUIP:按摩棒
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM93, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE94
;泡踊り実行判定
SIF COMABLE_DEF(94)
	RETURN 0
;ハダカでないとダメ
SIF !CHECK_CLO("ハダカ")
	RETURN 0
;お風呂場プレイ中でプレイマットが無いとダメ
SIF TEQUIP:沐浴Play == 0
	RETURN 0
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;Ｗ押し倒し中ならオンナでないとダメ
SIF TEQUIP:Ｗ推到 && TALENT:男性
	RETURN 0
;シャワー中はダメ
SIF TEQUIP:淋浴
	RETURN 0
;バイブ挿入中はダメ
SIF TEQUIP:按摩棒
	RETURN 0
;アナルバイブ挿入中はダメ
SIF TEQUIP:肛用按摩棒
	RETURN 0
;クリキャップ使用中は不可
SIF TEQUIP:震动式阴蒂夹
	RETURN 0
;クリップローター使用中は不可
SIF TEQUIP:震动式乳头夹
	RETURN 0
;オナホール装着中はダメ
SIF TEQUIP:飞机杯
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞
	RETURN 0
;アイマスク使用中は不可
SIF TEQUIP:眼罩
	RETURN 0
;浣腸使用中は不可
SIF TEQUIP:灌肠
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM94, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE95
;足コキ実行判定
SIF COMABLE_DEF(95)
	RETURN 0
;ラミア、マーメイド等、足が無い人や脚が使えない状況だとダメ
SIF COND("脚の使用") == 0
	RETURN 0
;アイマスク使用中は不可
SIF TEQUIP:眼罩
	RETURN 0
IF TEQUIP:推到
	;サドっ気が必要
	SIF ABL:施虐倾向 == 0
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM95, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE96
;アナル奉仕実行判定
SIF COMABLE_DEF(96)
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM96, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE97
;ぱんつコキ実行判定
SIF COMABLE_DEF(97)
	RETURN 0
;ぱんつコキできるパンツじゃないとダメ
SIF CHECK_CLO("ぱんつコキ") == 0
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;ハーピーはダメ
;SIF TALENT:哈比
;	RETURN 0
;ラミア、マーメイド等、足が無い人はダメ
SIF COND("脚が無い")
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM97, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE98
;クンニさせる実行判定
SIF COMABLE_DEF(98)
	RETURN 0
;調教者がオトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;ボールギャグ使用中は不可
SIF TEQUIP:口塞
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM98, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE99
;シックスナイン実行判定
SIF COMABLE_DEF(99)
	RETURN 0
;解除はいつでも可能
SIF TEQUIP:６９式
	RETURN 1
CALL COM_ABLE91
IF RESULT == 0
	CALL COM_ABLE98
	SIF RESULT == 0
		RETURN 0
ENDIF
CALL COM_ABLE1
IF RESULT == 0
	CALL COM_ABLE4
	SIF RESULT == 0
		RETURN 0
ENDIF
;妖精だとダメ
SIF TALENT:妖精
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
SIF ITEM:底垫 == 0 && NOITEM == 0
	RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM99, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE100
;スク水コキ実行判定
SIF COMABLE_DEF(100)
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;スク水を着ていないとダメ
SIF !CHECK_BODYS("スク水")
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM100, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE101
RETURN 0

@COM_ABLE102
RETURN 0

@COM_ABLE103
RETURN 0

@COM_ABLE104
;ぱふぱふ実行判定
SIF COMABLE_DEF(104)
	RETURN 0
;オトコだとダメ
SIF TALENT:男性
	RETURN 0
;妖精はだめ
SIF TALENT:妖精
	RETURN 0
SIF CHECK_CLO("谷間露出") + CHECK_CLO("はだけ可") == 0
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM104, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE105
;オナホコキ実行判定
SIF COMABLE_DEF(105)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:飞机杯 == 0 && NOITEM == 0
	RETURN 0
;押し倒し中は技巧LV3必須
SIF TEQUIP:推到 && ABL:技巧 < 3
	RETURN 0
;調教者がオトコかふたなりじゃないとダメ
SIF PENIS(PLAYER) == 0
	RETURN 0
;ハーピーはダメ
SIF TALENT:哈比
	RETURN 0
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM105, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE106
;逆バイブ実行判定
SIF COMABLE_DEF(106)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:按摩棒 == 0 && NOITEM == 0
	RETURN 0
;オトコだとダメ
SIF TALENT:PLAYER:男性
	RETURN 0
;手が使えないとダメ
SIF COND("指の使用") == 0
	RETURN 0
;アイマスク使用中は不可
SIF TEQUIP:眼罩
	RETURN 0
;処女の時はマゾっ気が必要
IF TALENT:PLAYER:处女
	SIF ABL:受虐倾向 < 3 && TALENT:PLAYER:抖Ｓ == 0
		RETURN 0
ENDIF
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM106, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE107
;逆Ａバイブ実行判定
SIF COMABLE_DEF(107)
	RETURN 0
;アイテムを持っているか、アイテムなし設定でないとダメ
SIF ITEM:肛用按摩棒 == 0 && NOITEM == 0
	RETURN 0
;手が使えないとダメ
SIF COND("指の使用") == 0
	RETURN 0
;アイマスク使用中は不可
SIF TEQUIP:眼罩
	RETURN 0
;お風呂場プレイ中はプレイマットが無いとダメ
IF TEQUIP:沐浴Play
	SIF ITEM:底垫 == 0 && NOITEM == 0
		RETURN 0
ENDIF
;実行値フィルタ
IF CONFIG("実行値フィルタ") || FLAG:10
	CALL COM_ORDER_COM107, 1
	RETURN LIMIT(RESULT, 0, 1)
ENDIF
RETURN 1

@COM_ABLE108
RETURN 0

@COM_ABLE109
RETURN 0

@COM_ABLE110
RETURN 0

@COM_ABLE111
RETURN 0

@COM_ABLE112
RETURN 0

@COM_ABLE113
RETURN 0

@COM_ABLE114
RETURN 0



