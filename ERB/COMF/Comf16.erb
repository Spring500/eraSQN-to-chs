@COM16
;兜合わせ
CALL PRINT_TRAIN_NAME(SELECTCOM)

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = %STR:2%
;-------------------------------------------------
;射精チェック
;-------------------------------------------------
CALL SAMEN_CHECK

RETURN 1

@FLAG_COM16, ARG
SIF CHECK_CLO("Ｖ露出") == 0
	CALL SETFLAG, "ボトムを露出：テキスト無し", TARGET
;調教者のＰ⇔対象のＰの汚れが移動
CALL EQ_STAIN, TARGET, PLAYER, 2, 2


@SOURCE_COM16, ARG
VARSET LOCAL

LOCAL:20 += 50
LOCAL:21 += 100

LOCAL:3  = 500
LOCAL:8 = COM_ORDER_STAIN("Ｐ", "Ｐ")*50 + 60
LOCAL:11  = 800
LOCAL:12  = 600
LOCAL:13 = 500
LOCAL:14 = 50

;ABL:ＢＬ倾向をみる
SELECTCASE ABL:ＢＬ倾向
CASE 0
	LOCAL:4 = 200
	LOCAL:5 = 100
CASE 1
	LOCAL:4 = 250
	LOCAL:5 = 180
CASE 2
	LOCAL:4 = 300
	LOCAL:5 = 250
CASE 3
	LOCAL:4 = 350
	LOCAL:5 = 350
CASE 4
	LOCAL:4 = 400
	LOCAL:5 = 500
CASEELSE
	LOCAL:4 = 450
	LOCAL:5 = 800
ENDSELECT

;オトコかふたなり
IF PENIS(TARGET)
	;ABL:Ｃ感觉をみる
	LOCAL:0 = CALCF("Ｃ刺激", 1)
	LOCAL:0 = SOURCE_EXABL(LOCAL:0, 33, PLAYER)
ENDIF

CALL CALC, "調教ソース", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM16
IF SELECTCOM == PREVCOM && COND("前Ｔ調教者") == PLAYER
	PRINTFORMW %CALLNAME:PLAYER%と%CALLNAME:TARGET%は、%TEXTS("キス継続")%互いのペニスを擦り合わせ続けている…
ELSE
	PRINTFORMW %CALLNAME:PLAYER%と%CALLNAME:TARGET%は%TEXTS("キス継続")%互いのペニスを擦り合わせた…
ENDIF
