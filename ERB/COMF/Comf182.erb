@COM182
;押し倒す(作成途中)

PRINTL 押し倒す

CALL TRAIN_MESSAGE_B

SIF REFUSE_CHECK()
	RETURN 0

STR:0 = 押し倒す

;終了時は修正なし
IF TEQUIP:PLAYER:推到
	TEQUIP:PLAYER:推到 = 0
	RETURN 1
ELSE
	TEQUIP:PLAYER:推到 = 1
ENDIF

RETURN 1

@FLAG_COM182, ARG

@SOURCE_COM182, ARG
VARSET LOCAL

LOCAL:20 += 10
LOCAL:21 += 50

LOCAL:9 = 1000
LOCAL:11 = 1000
LOCAL:13 = 1000

;欲望を見る
SELECTCASE ABL:欲望
CASE 0
	TIMES LOCAL:9 , 2.00
	TIMES LOCAL:11 , 0.50
CASE 1
CASE 2
	TIMES LOCAL:9 , 0.70
	TIMES LOCAL:11 , 1.50
CASE 3
	TIMES LOCAL:9 , 0.30
	TIMES LOCAL:11 , 2.00
CASE 4
	TIMES LOCAL:9 , 0.05
	TIMES LOCAL:11 , 3.00
CASEELSE
	TIMES LOCAL:9 , 0.00
	TIMES LOCAL:11 , 5.00
ENDSELECT

CALL CALC, "調教ソース", LOCAL:0, LOCAL:1, LOCAL:2, LOCAL:3, LOCAL:4, LOCAL:5, LOCAL:6, LOCAL:7, LOCAL:8, LOCAL:9, LOCAL:10, LOCAL:11, LOCAL:12, LOCAL:13, LOCAL:14, LOCAL:15, LOCAL:16, LOCAL:17, LOCAL:18, LOCAL:19, LOCAL:20, LOCAL:21, ARG


@TRAIN_MESSAGE_COM182
PRINTFORMW %CALLNAME:PLAYER%は%CALLNAME:TARGET%を押し倒した！


@EQUIP_COM27
;押し倒し中
CALL SOURCE_COM182, 20

RETURN 1

@EQUIP_COM27_2
;押し倒され中
PRINTL ＜押し倒し中＞

RETURN 1
