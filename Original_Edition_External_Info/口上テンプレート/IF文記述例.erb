IF A
	Aを満たすと実行されます。
	IF RAND:2 == 0
		Aを満たすと、2分の1の確率で実行されます。
	ELSE
		Aを満たすと、2分の1の確率で実行されます。
	ENDIF
	Aを満たすと実行されます。
	IF RAND:3 == 0
		Aを満たすと、3分の1の確率で実行されます。
	ELSEIF RAND:2 == 0
		Aを満たすと、3分の1の確率で実行されます。
	ELSE
		Aを満たすと、3分の1の確率で実行されます。
	ENDIF
	Aを満たすと実行されます。
ELSEIF B
	Aを満たさず、Bを満たすと実行されます。
	IF D
		Aを満たさず、BとDを満たすと実行されます。
	ELSEIF E
		Aを満たさず、Bを満たし、Dを満たさず、Eを満たすと実行されます。
	ELSE
		Aを満たさず、Bを満たし、DとEを満たさないと実行されます。
	ENDIF
	Aを満たさず、Bを満たすと実行されます。
ELSEIF C
	AとBを満たさず、Cを満たすと実行されます。
	IF F
		AとBを満たさず、CとFを満たすと実行されます。
		IF G
			AとBを満たさず、CとFとGを満たすと実行されます。
		ENDIF
		AとBを満たさず、CとFを満たすと実行されます。
	ENDIF
	AとBを満たさず、Cを満たすと実行されます。
ELSE
	AとBとCを満たさないと実行されます。
ENDIF
（一番最後の行は必ず改行しておいて下さい。）